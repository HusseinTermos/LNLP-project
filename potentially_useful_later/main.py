        # old_path = Path(config["data"]["processed_dataset_path"])
        # new_path = old_path.with_name(f"{old_path.stem}_{run_id}{old_path.suffix}")
        # config["data"]["processed_dataset_path"] = new_path

        # print(f"\n--- Training run number {i} ---")

        # print(f"\n--- Building RAG ---")
        # build_dataset_from_config(config)

        # print(f"\n--- Training ---")
        # run_train_then_test(config)
